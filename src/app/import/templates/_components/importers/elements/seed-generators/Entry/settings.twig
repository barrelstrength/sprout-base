{% import "_includes/forms" as forms %}

{{ forms.selectField({
    label: "Section"|t('sprout-base'),
    instructions: "The section to import content."|t('sprout-base'),
    required: true,
    name: "settings[channel]",
    options: [{label: "Select a section..."|t('sprout-base'), value: null}]|merge(channels),
    value: seedJob.settings.channel ?? null,
    errors: seedJob.getErrors('settings') ?? null
}) }}