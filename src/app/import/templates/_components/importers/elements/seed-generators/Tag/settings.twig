{% import "_includes/forms" as forms %}

{{ forms.selectField({
    label: "Tag Group"|t('sprout-base'),
    instructions: "The tag group to import content."|t('sprout-base'),
    required: true,
    name: "settings[tagGroup]",
    options: [{label: "Select a group..."|t('sprout-base'), value: null}]|merge(tagGroups),
    value: seedJob.settings.tagGroup ?? null,
    errors: seedJob.getErrors('tagGroup') ?? null
}) }}
