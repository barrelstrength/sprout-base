{% import "_includes/forms" as forms %}
{% import "sprout-base-metadata/_includes/sproutseo" as sprout %}

{% set sproutSeoApp = craft.app.getModule('sprout-seo').app %}

{% set transformOptions = sprout.seo.getTransforms() %}

<div class="sproutseo-box-opengraph">

    <div class="sproutseo-fields" id="open-graph">

        {% set ogTypesOptions = {
            '' : 'Select...'|t('sprout'),
            'article' : 'Article'|t('sprout'),
            'website' : 'Website'|t('sprout'),
        } %}

        {{ forms.selectField({
            label: 'Open Graph Type'|t('sprout'),
            instructions: 'First, select your Open Graph Type. Learn more about <a href="https://developers.facebook.com/docs/reference/opengraph#object-type" target="_blank">How Open Graph Stories work.</a>'|t('sprout'),
            name: field.handle~'[metadata][ogType]',
            options: ogTypesOptions,
            value: meta.ogType,
            errors: "",
            required: false
        }) }}

        {{ forms.textField({
            label: "Title"|t('sprout'),
            instructions: 'The Title of your content which will appear in Open Graph metadata.'|t('sprout'),
            name: field.handle~"[metadata][ogTitle]",
            value: meta.ogTitle
        }) }}

        {{ forms.textareaField({
            label: "Description"|t('sprout'),
            instructions: 'A description of your content which will appear in Open Graph metadata.'|t('sprout'),
            name: field.handle~"[metadata][ogDescription]",
            value: meta.ogDescription
        }) }}

        {% set ogImageId = sproutSeoApp.optimize.getImageId(meta.ogImage ?? null) %}
        {% if ogImageId %}
            {% set ogImageElement = [craft.app.elements.getElementById(ogImageId)] %}
        {% else %}
            {% set ogImageElement = [] %}
        {% endif %}

        {{ forms.field({
            label: 'Image'|t('sprout'),
            instructions: 'An image that represents your content that will display in Open Graph metadata.'|t('sprout'),
        }, forms.elementSelect({
            elements: ogImageElement,
            sourceElementId: [meta.ogImage],
            elementType: 'craft\\elements\\Asset',
            sources: '*',
            id: 'metaDetailsOgImage',
            name: field.handle~'[metadata][ogImage]',
            jsClass: 'Craft.AssetSelectInput',
            addButtonLabel: 'Select Image'|t('sprout-forms'),
            limit: 1,
            criteria: {kind: ['image'], siteId : null}
        })) }}

    </div>

</div>





