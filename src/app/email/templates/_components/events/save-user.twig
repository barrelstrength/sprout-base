{% import "_includes/forms" as forms %}

{{ forms.checkboxField({
    label: "When user is created?"|t('sprout'),
    name: "whenNew",
    checked: event.whenNew
}) }}

{{ forms.checkboxField({
    label: "When user is updated?"|t('sprout'),
    name: "whenUpdated",
    checked: event.whenUpdated
}) }}

{% if event.groups|length %}

    {{ forms.checkboxSelectField({
        label: "User Groups <span class='info'>Select which user groups will trigger the notification.</span>"|t('sprout'),
        name: "userGroupIds",
        options: event.groups,
        values: event.userGroupIds,
        errors: event is defined ? event.getErrors('userGroupIds') : null,
        showAllOption: true
    }) }}

{% else %}

    <p>
        <em>{{ "Add a User Group to limit this event by User Group."|t('sprout') }}</em>
    </p>

{% endif %}

{{ forms.checkboxField({
    label: "Send to Admins?"|t('sprout'),
    name: "adminUsers",
    checked: event.adminUsers
}) }}

