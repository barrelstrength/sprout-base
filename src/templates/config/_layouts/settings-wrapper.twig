{% extends "sprout/config/_layouts/base-settings" %}
{% import "sprout/config/_includes/sproutcp" as sproutCp %}

{% block actionButton %}

    {% set actionButtonTemplate = currentSubsection.actionButtonTemplate ?? null %}

    {% if actionButtonTemplate %}
        {% include actionButtonTemplate %}
    {% endif %}

    {% if sproutConfig.isUpgradable() %}
        {{ sproutCp.upgradeToProButton({
            url: sproutConfig.getUpgradeUrl()
        }) }}
    {% endif %}

{% endblock %}

{% block content %}

    {% include currentSubsection.template %}

{% endblock %}
