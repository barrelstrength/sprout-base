{% extends "_layouts/cp" %}

{% do view.registerAssetBundle("barrelstrength\\sproutbase\\app\\forms\\web\\assets\\cp\\CpAsset") %}

{% set title  = "Settings"|t('sprout') %}
{% set bodyClass = craft.app.request.getSegments()|slice(0, 3)|join(' ') %}

{% set subnav = {
    entries: {
        label: "Entries"|t('sprout'),
        url: cpUrl('sprout/forms/entries')
    },
    forms: {
        label: "Forms"|t('sprout'),
        url: cpUrl('sprout/forms')
    }
} %}

{% if currentUser.admin %}
    {% set subnav = subnav|merge({
        settings: {
            label: "Settings"|t('sprout'),
            url: cpUrl('sprout/settings/forms/general')
        }
    }) %}
{% endif %}

{% set selectedSubnavItem = craft.app.request.getSegment(2) %}
{% set selectedSidebarItem = craft.app.request.getSegment(3) %}

{% set sidebar %}

    <nav class="last">
        <ul>
            <li><a href="{{ cpUrl('sprout/settings/forms/general') }}"
                    {% if selectedSidebarItem == 'general' %}class="sel"{% endif %}>{{ "General"|t('sprout') }}</a>
            </li>

            <li><a href="{{ cpUrl('sprout/settings/forms/entrystatuses') }}"
                    {% if selectedSidebarItem == 'entrystatuses' %}class="sel"{% endif %}>{{ "Entry Statuses"|t('sprout') }}</a>
            </li>

            <li><a href="{{ cpUrl('sprout/settings/forms/advanced') }}"
                    {% if selectedSidebarItem == 'advanced' %}class="sel"{% endif %}>{{ "Advanced"|t('sprout') }}</a>
            </li>

            <li class="heading"><span>Examples</span></li>

            <li><a href="{{ cpUrl('sprout/settings/forms/examples') }}"
                    {% if selectedSidebarItem == 'examples' %}class="sel"{% endif %}>{{ "Form
                    Templates"|t('sprout') }}</a>
            </li>
        </ul>
    </nav>

{% endset %}