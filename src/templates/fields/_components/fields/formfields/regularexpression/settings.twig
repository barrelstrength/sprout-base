{% import "_includes/forms" as forms %}

{{ forms.textField({
    label: "Placeholder Text"|t('sprout'),
    instructions: "The text that will be shown if the field doesn’t have a value."|t('sprout'),
    id: 'placeholder',
    name: 'placeholder',
    value: field.placeholder
}) }}

<div id="sproutregularexpressionfield-custom-validation-pattern">
    {{ forms.textField({
        label: 'Validation Pattern'|t('sprout'),
        instructions: 'The regular expression to use to validate this field.'|t('sprout'),
        name: 'customPattern',
        placeholder: '(.*)',
        value: field.customPattern,
        errors: field.getErrors('customPattern')
    }) }}

    {{ forms.textField({
        label: 'Error Message'|t('sprout'),
        instructions: 'Display this error message when validation fails.'|t('sprout'),
        name: 'customPatternErrorMessage',
        placeholder: 'Invalid format.'|t('sprout'),
        value: field.customPatternErrorMessage
    }) }}
</div>

